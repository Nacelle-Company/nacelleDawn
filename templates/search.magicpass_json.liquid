{% comment %}
=======================================================
This file is managed by MagicPass. Do not modify.
To remove, please click "Remove from store" in our app.
https://apps.shopify.com/password-protected-pages
=======================================================
{% endcomment %}


{% layout none %}
[{% for result in search.results %}
  {%- comment %}<locksmith:50a0>{% endcomment -%}
    {%- capture var %}{% render 'locksmith-variables', scope: 'subject', subject: result, subject_parent: search, variable: 'transparent' %}{% endcapture %}{% if var == "true" %}{% else %}{% continue %}{% endif -%}
  {%- comment %}</locksmith:50a0>{% endcomment -%}
  {% if result.id != search.results.last.id %}
    { "id": {{ result.id | json }}, "title": {{ result.title | json }} },
  {% else %}
    { "id": {{ result.id | json }}, "title": {{ result.title | json }} }
  {% endif %}
{% endfor %}]
